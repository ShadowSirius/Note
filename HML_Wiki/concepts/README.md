# Concept Articles (`concepts/`)

This directory contains the core structured wiki.

- Articles are clean, focused concept pages.
- Every note should be interlinked using typed wikilinks to define relationships (e.g., `[[OtherNote|text @supports]]`, `[[OtherNote|text @contradicts]]`).
- These relationships are compiled into frontmatter automatically by the synchronization script.
